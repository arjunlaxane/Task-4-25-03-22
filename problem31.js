//31.write a code to count the elements in the array . Don’t use length property

let count = 0;
let myarray = [11, 22, 33, 44, 55];
for (let i = 0; i < myarray.length; i++) {
  if (myarray) {
    count++;
  }
}
console.log(count);
